'use client';

import React from 'react';

// The template does not have a dedicated "Contact" section with a form or multiple links.
// It only has an email button in the footer.
// This component is included to satisfy the prompt's request for a Contact.tsx,
// but its content will be minimal or empty if the template doesn't support it.
// The footer already handles the email contact.
// As per rule 8: "NO HALLUCINATED CTAs: Do NOT add a 'Let's Connect', 'Let's connect',
// 'Get In Touch', 'Get in touch', 'Contact Me', 'Hire Me', 'Work With Me', or any similar
// call-to-action section, button, or banner unless that EXACT text and structure exists in index.html."
// The template's index.html does not have a standalone contact section.
// Therefore, this component will render nothing to adhere to the template fidelity.
// The contact email is handled in the Footer component.

const Contact: React.FC = () => {
  return null; // Render nothing as per template fidelity rules
};

export default Contact;